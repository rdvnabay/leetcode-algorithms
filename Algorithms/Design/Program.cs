// Demo runner for 1 solution files; the README is the canonical ID-sorted index.
using Design.Easy;

DesignParkingSystem parkingSystem = new(1, 1, 0);
parkingSystem.AddCar(1);
parkingSystem.AddCar(2);
parkingSystem.AddCar(3);
parkingSystem.AddCar(1);