namespace Algorithms.Solutions.Easy;

public class TeemoAttacking
{
    public static int Run(int[] timeSeries, int duration)
    {
        return 0;
        //List<int> result = new();

        //for (int i = 0; i < timeSeries.Length; i++)
        //{
        //    for (int j = timeSeries[i]; j < timeSeries[i] + duration; j++)
        //    {
        //        if (!result.Contains(j))
        //            result.Add(j);
        //    }
        //}
        //return result.Count;
    }
}
