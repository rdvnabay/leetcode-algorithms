namespace HashTable.Easy;

public static class SubarraysDistinctElementSumOfSquaresI
{
    public static int Run(IList<int> nums)
    {
        //1, 2, 1
        int range = 1;

        int start = 0;
        int end = 0;
        int i = 0;

        //while (range < nums.Count)
        //{

        //}
        return 0;
    }
}
