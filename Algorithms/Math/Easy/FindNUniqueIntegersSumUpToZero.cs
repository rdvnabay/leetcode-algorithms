namespace Math.Easy;

public static class FindNUniqueIntegersSumUpToZero
{
    public static int[] Run(int n)
    {
        int[] result = new int[n];

        //n = n / 2;

        for (int i = 0; i <= n / 2; i++)
        {
            var test = i;
            //result[i]=
        }
        bool isEven = n % 2 == 0;

        return null;
    }
}
